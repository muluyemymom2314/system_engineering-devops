#!/usr/bin/env bash
# A Bash script that forcibly kills the 7-highlander process using SIGKILL

# Find the PID of the running 7-highlander process
pid=$(pgrep -f "7-highlander")

# Check if a PID was found
if [ -n "$pid" ]; then
    # Send SIGKILL (signal 9) to the process
    kill -9 "$pid"
fi
